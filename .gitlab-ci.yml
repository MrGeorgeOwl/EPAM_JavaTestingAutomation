variables:
    POM_PATH: ./aircompany-master/Java/pom.xml

test:
    tags:
        - java8
        - maven
    script:
        - echo "Executing tests"
        - ls -l && ls ./aircompany-master/
        - cd /aircompany-master/Java/src
        - mvn -f $POM_PATH clean test
